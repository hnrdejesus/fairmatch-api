spring:
  application:
    name: fairmatch-api

  datasource:
    url: jdbc:postgresql://localhost:5433/baba
    username: baba
    password: baba123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect